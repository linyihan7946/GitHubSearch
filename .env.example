# GitHubSearch 环境变量
# 复制此文件为 .env 后根据需要修改

# 服务端口（默认 3000，与 docker-compose.yml 一致）
PORT=3000

# GitHub Personal Access Token（可选）
# 配置后可显著提升 API 请求限流：
#   - 未认证：10 次/分钟（Search API）
#   - 已认证：30 次/分钟（Search API）
# 生成地址：https://github.com/settings/tokens
# 仅需勾选 public_repo 读取权限即可
GITHUB_TOKEN=
