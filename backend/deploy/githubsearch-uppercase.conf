# GitHubSearch 项目专属兼容入口。
# 一键部署生成的规范路径是 /githubsearch/；旧的大写地址永久跳转到规范路径。
location = /GitHubSearch {
    return 301 /githubsearch/;
}

location ^~ /GitHubSearch/ {
    rewrite ^/GitHubSearch/(.*)$ /githubsearch/$1 permanent;
}
